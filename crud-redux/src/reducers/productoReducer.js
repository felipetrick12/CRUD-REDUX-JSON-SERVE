

const initialState = {

    products : [],
    error: null,
    loading: false,
  

}

export const productoReducer = ( state = initialState, action )=> {

    switch( action.type ) {
        

        default: 

        return state
    }
}